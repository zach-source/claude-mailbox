# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.pytest_cache/

# beads / dolt (this repo's workspace connects to the shared server; no local data)
.beads/proxieddb/
.beads/*.db
.beads/backup/
.dolt/
.beads-credential-key

# Beads / Dolt files (added by bd init)
*.db
