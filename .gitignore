# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
uv.lock

# beads workspace: machine-local connection config to the shared dolt server.
# Set up per machine with `bd init --global` (see README). Not committed.
.beads/
.dolt/
*.db
.beads-credential-key
